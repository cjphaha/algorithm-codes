#include<stdio.h>
int main(void)
{
  int a,b,i,j,k,r[1000];//定义变量
  while(scanf("%d",&a)!=EOF）
  {
    r[b++]=a;
    }
for(i=b-1;i>=o;i--)
  {
    for(j=0;j<=i;j++)
    {
      if(r[j]>=r[j++])
      {
        b=r[j];r[j]=r[j+1];r[j+1]=b;
        }
      else continue;
     }
   }
   for(i=0;i<=b-1;i++)
   printf("%d",a[i]);
   return 0;
   getchar();
   getchar();
   }
